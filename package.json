{
  "name": "bonae",
  "private": true,
  "version": "1.0.0",
  "description": "BONAE TECH static marketing site",
  "scripts": {
    "dev": "npm --prefix apps/static run dev",
    "build": "npm --prefix apps/static run build",
    "preview": "npm --prefix apps/static run preview"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  }
}
