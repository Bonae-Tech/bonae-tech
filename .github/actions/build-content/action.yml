name: Build content package
description: Install dependencies and build @bonae/content (required before apps, workers, or services)

runs:
  using: composite
  steps:
    - name: Install and build packages/content
      shell: bash
      run: npm ci && npm run build
      working-directory: packages/content
