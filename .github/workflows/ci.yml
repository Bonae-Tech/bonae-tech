name: CI

on:
  push:
    paths:
      - 'apps/static/**'
      - 'apps/admin/**'
      - 'packages/content/**'
      - 'services/content-api/**'
      - '.github/workflows/ci.yml'
  pull_request:
    paths:
      - 'apps/static/**'
      - 'apps/admin/**'
      - 'packages/content/**'
      - 'services/content-api/**'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            packages/content/package-lock.json
            apps/static/package-lock.json
            apps/admin/package-lock.json
            services/content-api/package-lock.json

      - run: npm ci
        working-directory: packages/content
      - run: npm run build
        working-directory: packages/content
      - run: npm run validate:published
        working-directory: packages/content

      - run: npm ci
        working-directory: apps/static
      - run: npm run build
        working-directory: apps/static

      - run: npm ci
        working-directory: apps/admin
      - run: npm run build
        working-directory: apps/admin

      - run: npm ci
        working-directory: services/content-api
      - run: npm run build
        working-directory: services/content-api
